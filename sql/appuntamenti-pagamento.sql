-- =============================================================================
-- Dopo aver aggiunto la colonna Pagamento (boolean) in Table Editor,
-- esegui questo script per farla vedere all'app.
-- Supabase: SQL Editor > Run
-- =============================================================================

notify pgrst, 'reload schema';
